# Homebrew formula

Source of truth for the formula. Publish by copying to
[`latentmeta/homebrew-tap`](https://github.com/latentmeta/homebrew-tap) as `Formula/zerum.rb`.

Checksums come from the GitHub Release `SHA256SUMS` file.

```bash
brew install latentmeta/tap/zerum
```
